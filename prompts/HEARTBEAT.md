# Heartbeat Tasks

心跳服务周期性检查的任务列表。

## 规则

- 心跳服务每 30 分钟检查一次此文件
- LLM 根据任务描述判断是否有需要执行的任务
- 返回 `run` 时执行任务，返回 `skip` 时跳过

## 每日检查

- 检查是否有未处理的错误日志
- 确认关键服务的运行状态

## 定时任务

- 每小时检查备份文件是否存在
- 每 30 分钟检查磁盘空间使用率

## 条件触发

- 如果发现异常，记录到日志文件
- 如果超过阈值，发送告警通知